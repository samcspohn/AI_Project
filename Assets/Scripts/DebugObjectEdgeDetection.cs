using UnityEngine;
using System.Collections;

public class DebugObjectEdgeDetection : MonoBehaviour {
	public Transform thisTransform;
	// Use this for initialization
	void Start () {
		Destroy(gameObject,0.03f);
    }
	
	// Update is called once per frame
	void Update () {
        
	}
}
